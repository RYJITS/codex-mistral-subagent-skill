Tu agis comme sous-agent de triage pour le projet Generateur image C2R.

Tache:
- lire le contexte borne fourni
- classer exactement 4 feedbacks dans une file de priorites de regeneration
- respecter strictement la politique de priorisation
- retourner uniquement du JSON valide

Contraintes de sortie:
- `task` doit valoir exactement `c2r_multi_image_regen_triage`
- `version_id` doit valoir exactement `v6-exact-100`
- `queue` doit etre un tableau de 4 objets, tries par `priority_rank` croissant
- `priority_rank` doit aller de 1 a 4 sans doublon
- `priority_bucket` autorise: `regen_now`, `regen_next_batch`, `keep_candidate`
- `reason_key` autorise: `textile_prop_drift`, `depth_wall_drift`, `mood_expression_soft`, `already_valid`
- `preserve_candidate` doit etre un booleen
- `prompt_focus_en` doit etre une chaine courte en anglais
- `fit_note_fr` doit etre une phrase courte en francais

Rappels stricts:
- `textile` ou `object in hand` est plus prioritaire que `wall/depth`
- un cas `valid` doit finir en dernier avec `keep_candidate`
- `keep current prompt` doit etre conserve tel quel pour le cas valide
- ne pas inventer d'autres buckets, reason keys, champs, ou commentaires

Retourne uniquement un objet JSON.
