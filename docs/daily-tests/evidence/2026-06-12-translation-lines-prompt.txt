Translate each numbered English line into French.

Return only valid JSON with this exact shape:

{
  "title_fr": "string",
  "summary_fr": "string",
  "lines": [
    { "line_no": 1, "fr": "string" },
    { "line_no": 2, "fr": "string" },
    { "line_no": 3, "fr": "string" },
    { "line_no": 4, "fr": "string" },
    { "line_no": 5, "fr": "string" },
    { "line_no": 6, "fr": "string" },
    { "line_no": 7, "fr": "string" }
  ],
  "preserved_literals": ["string"]
}

Rules:
- Translate only the meaning of each source line.
- Preserve every backticked literal exactly.
- Keep the same line numbers.
- Use concise ASCII French.
- `title_fr` must describe a French public quickstart note.
- `summary_fr` must say that the note helps localize public repo docs while preserving exact operational strings.
- `preserved_literals` must contain exactly these literals:
  - `mistral-subagent`
  - `MISTRAL_API_KEY`
  - `mistral-small-latest`
  - `mistral-medium-3.5`
  - `mistral-large-latest`
  - `node mistral-subagent/scripts/mistral-subagent.mjs check`
  - `node mistral-subagent/scripts/mistral-subagent.mjs run`
